// App.tsx is intentionally minimal — routing is handled by router.tsx + AppShell.
// This file exists for compatibility but is not rendered directly.
export default function App() {
  return null;
}
